command: ["sh", "-c", "cd frontend && npm install && npm run build && cd .. && pip install -r backend/requirements.txt && cd backend && ../.venv/bin/uvicorn app:app --host 0.0.0.0 --port $DATABRICKS_APP_PORT"]
